# backend/dev.hcl -- generated by pipeline-automator scaffold (project init)
# Backend configuration for the dev state of payroll_pipeline.
# Passed to `terraform init -backend-config=backend/dev.hcl` by the
# deploy workflow. The environment segment in `key` is what keeps environments
# in separate state files -- do not remove it.

bucket         = "payroll_pipeline_tfstate"
key            = "payroll_pipeline/dev/terraform.tfstate"
region         = "eu-west-1"
dynamodb_table = "terraform-locks"
encrypt        = true
